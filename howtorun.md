# How to run Agenza

Open a terminal in the Agenza project folder.

Install the packages the first time:

```powershell
npm install
```

Run the app:

```powershell
npm run dev
```

Close the Agenza window or press `Ctrl+C` in the terminal to stop it.
